# Mail-in-a-Box Docker Environment Configuration
# Copy this file to .env and customize the values for your setup

# Primary hostname for the mail server (REQUIRED)
# This should be a fully qualified domain name that points to your server
PRIMARY_HOSTNAME=mail.aurakl.ai

# Public IP addresses (will be auto-detected if not set)
# Set these if auto-detection doesn't work in your environment
PUBLIC_IP=
PUBLIC_IPV6=

# Private IP addresses (usually auto-detected)
PRIVATE_IP=
PRIVATE_IPV6=

# Storage configuration
STORAGE_USER=user-data
STORAGE_ROOT=/home/user-data

# Timezone (default: UTC)
TZ=UTC

# Optional: Database configuration (if using external database)
# DB_ROOT_PASSWORD=secure_root_password
# DB_NAME=mailinabox
# DB_USER=mailinabox
# DB_PASSWORD=secure_password

# Optional: Let's Encrypt email for SSL certificates
# LETSENCRYPT_EMAIL=admin@example.com

# Optional: Backup configuration
# BACKUP_ENCRYPTION_PASSWORD=your_backup_password
# BACKUP_TARGET=s3://your-bucket/path
